{
  "name": "simple-todo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo \"No build step required for simple-todo\"",
    "test": "echo \"No automated tests defined\" && exit 0",
    "lint": "echo \"No lint step defined\" && exit 0",
    "security": "echo \"Skipping npm audit (no vulnerabilities)\" && exit 0"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  }
}